local config = {
    debugEnabled = false
}

local description = {
    debugEnabled = "Enables debugging tools used to work on this mod.  Shouldn't be enabled for regular use."
}

return config, description
